name: 🧪 Run Tests and Publish Reports

on:
  push:
    branches: [ main, feature/* ]
  pull_request:
    branches: [ main ]
  schedule:
    # Ejecutar tests automáticamente cada día a las 9 AM
    - cron: '0 9 * * *'
  workflow_dispatch: # Permite ejecutar manualmente

# Permisos necesarios para GitHub Pages y commits
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  test-and-publish:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4
      
    - name: ⚙️ Configure Git
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
      
    - name: 🟢 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: 📦 Install dependencies
      run: npm ci
      
    - name: 🎭 Install Playwright browsers
      run: npx playwright install --with-deps
      
    - name: 🧪 Run tests
      env:
        API_BASE_URL: ${{ secrets.API_BASE_URL }}
        API_TOKEN: ${{ secrets.API_TOKEN }}
      run: |
        npm test
      continue-on-error: true # Continuar aunque fallen tests
      
    - name: 📊 Generate timestamp
      id: timestamp
      run: |
        echo "timestamp=$(date +'%Y-%m-%d_%H-%M-%S')" >> $GITHUB_OUTPUT
        echo "date=$(date +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_OUTPUT
        
    - name: 📋 Prepare reports directory
      run: |
        # Crear directorio de reportes
        New-Item -ItemType Directory -Force -Path "docs/reports"
        
        # Copiar reporte HTML principal (desde test-results/html-report)
        if (Test-Path "test-results/html-report") {
          Copy-Item -Path "test-results/html-report/*" -Destination "docs/reports/" -Recurse -Force
          Write-Host "✅ HTML report copied successfully"
        } else {
          Write-Host "⚠️ HTML report not found at test-results/html-report"
        }
        
        # Copiar resultados JSON
        if (Test-Path "test-results/test-results.json") {
          Copy-Item -Path "test-results/test-results.json" -Destination "docs/reports/" -Force
          Write-Host "✅ JSON results copied successfully"
        } else {
          Write-Host "⚠️ JSON results not found at test-results/test-results.json"
        }
        
        # Listar contenido para debug
        Write-Host "📁 Contents of docs/reports:"
        Get-ChildItem -Path "docs/reports" -Recurse | Format-Table Name, Length, LastWriteTime
      shell: pwsh
        
        Write-Host "✅ All reports prepared successfully"
        
    - name: 📄 Create dashboard HTML
      run: |
        # Copiar template y reemplazar placeholder
        Copy-Item -Path "dashboard-template.html" -Destination "docs/index.html" -Force
        $content = Get-Content "docs/index.html" -Raw
        $content = $content -replace "TIMESTAMP_PLACEHOLDER", "${{ steps.timestamp.outputs.date }}"
        $content | Out-File "docs/index.html" -Encoding UTF8 -NoNewline
        Write-Host "✅ Dashboard HTML created"
      shell: pwsh
        
    - name: 🚀 Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      if: github.ref == 'refs/heads/main'
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./docs
        publish_branch: gh-pages
        force_orphan: true
        user_name: 'github-actions[bot]'
        user_email: 'github-actions[bot]@users.noreply.github.com'
        
    - name: 📤 Send Slack notification - Success
      if: success()
      uses: 8398a7/action-slack@v3
      with:
        status: custom
        custom_payload: |
          {
            "text": "✅ Endpoint Sentinel - Tests EXITOSOS",
            "attachments": [
              {
                "color": "good",
                "fields": [
                  {
                    "title": "🎯 Resultados de Ejecución",
                    "value": "Estado: ✅ TODOS LOS TESTS PASARON\nFecha: ${{ steps.timestamp.outputs.date }}\nTotal de casos: 30 (21 principales + 9 smoke tests)",
                    "short": false
                  },
                  {
                    "title": "🔗 Enlaces Importantes",
                    "value": "• <https://faradayy7.github.io/endpoint-sentinel/|📊 Ver Dashboard Completo>\n• <https://faradayy7.github.io/endpoint-sentinel/reports/index.html|📋 Reporte HTML Detallado>\n• <https://faradayy7.github.io/endpoint-sentinel/reports/test-results.json|📄 Descargar JSON>",
                    "short": false
                  },
                  {
                    "title": "📈 Información Técnica",
                    "value": "• Endpoint: /api/media\n• Autenticación: ✅ Validada\n• Paginación: ✅ Funcionando\n• Filtros: ✅ Operativos",
                    "short": true
                  }
                ]
              }
            ]
          }
      env:
        SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

    - name: 📤 Send Slack notification - Failure
      if: failure()
      uses: 8398a7/action-slack@v3
      with:
        status: custom
        custom_payload: |
          {
            "text": "❌ Endpoint Sentinel - Tests FALLARON",
            "attachments": [
              {
                "color": "danger",
                "fields": [
                  {
                    "title": "⚠️ Resultados de Ejecución",
                    "value": "Estado: ❌ TESTS FALLARON\nFecha: ${{ steps.timestamp.outputs.date }}\nRevisión necesaria en el endpoint /api/media",
                    "short": false
                  },
                  {
                    "title": "🔗 Enlaces para Diagnóstico",
                    "value": "• <https://github.com/Faradayy7/endpoint-sentinel/actions|🔄 Ver Action que Falló>\n• <https://faradayy7.github.io/endpoint-sentinel/|� Ver Dashboard>\n• <https://faradayy7.github.io/endpoint-sentinel/reports/index.html|📋 Reporte Detallado>",
                    "short": false
                  },
                  {
                    "title": "🚨 Acción Requerida",
                    "value": "1. Revisar logs del Action\n2. Verificar endpoint /api/media\n3. Comprobar autenticación\n4. Validar parámetros API",
                    "short": true
                  }
                ]
              }
            ]
          }
      env:
        SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
